const moduleCodemap = require('@xsy-customize/errorcode').moduleCode

module.exports = (moduleName) => (ctx, next) => {
  return new Promise(function(resolve, reject) {
    const moduleCode = moduleCodemap[moduleName];
    if (moduleCode) ctx.state.moduleCode = moduleCode;
    resolve(next())
  });
}
